{
  "name": "sylvia-dev",
  "build": {
    "dockerfile": "Dockerfile.dev",  // path from context root
    "context": ".."
  },
  "postCreateCommand": "pip install -r requirements.txt",  // no ../ needed anymore
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-azuretools.vscode-docker"
      ]
    }
  }
}
